{
  "name": "root",
  "workspaces": [
    "components",
    "demos/*"
  ],
  "scripts": {
    "dev": "run-p \"wc -- dev\" \"static -- dev\" \"nextjs -- dev\"",
    "wc": "npm run -w components --",
    "static": "npm run -w static --",
    "nextjs": "npm run -w nextjs --",
    "format": "prettier -w .",
    "clean": "shx rm -rf ./components/dist",
    "predev": "npm run clean"
  },
  "devDependencies": {
    "fast-glob": "^3.2.11",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.6.2",
    "prettier-plugin-astro": "^0.0.12",
    "shx": "^0.3.4"
  }
}
